export default class GeoObjectOptions {
  constructor({ type, typePoint, color, opacity, zIndex }) {
    this.type = type;
    this.typePoint = typePoint;
    this.color = color;
    this.opacity = opacity;
    this.zIndex = zIndex;
  }
}
